# 告警中心与消防联动系统优化说明文档

本说明文档记录了告警中心分类计数统计、火警行动态火星粒子动效，以及灭火联动中心 1301 气体/高压细水雾自动与手动工艺流程的重构成果。

---

## 优化变更内容

### 1. 告警中心分类计数与智能过滤控制台
- **交互设计**：在告警控制台的筛选栏上方新增了四个发光玻璃态（Glassmorphism）统计卡片，分别展示系统的“总数记录”、“火警数量”、“故障数量”和“状态数量”。
- **双向联动**：这些统计卡片能够随着全局数据状态动态刷新。点击任意统计卡片会直接激活对应的类型过滤。例如，点击“火警数量”卡片可以快速筛选所有的火灾报警，点击“总数记录”卡片可一键清除过滤。
- **微动效**：每个卡片配有微幅上浮悬停效果、状态灯柱，以及火警分类独有的红光呼吸闪烁点，极富科技感。

### 2. 火警信息行动态火星粒子动效
- **视觉增强**：在 `AlarmCenter.tsx` 中，当信息项被判定为火灾警报（`type === "fire"`）时，行内被植入了 5 个随机左右漂浮、逐渐上升并微缩渐隐的红黄色火星粒子。
- **动效实现**：通过 CSS `@keyframes sparkRise` 配合 inline variables（自定义偏移与漂移量 `--drift`）实现，火星粒子的移动表现流畅，极大地增强了视觉紧急感。
- **紧急发光**：当选中火灾行时，行体表现为高亮红渐变呼吸发光框，与常规告警和故障告警做出了显著的差异化排版。

### 3. 灭火联动工作台全面重构
灭火联动工作台依据消防控制逻辑进行了全面重构，不仅支持 1301 气体灭火和高压细水雾灭火两种物理介质，还支持自动（AUTO）和手动（MAN）两种控制时序：
- **1301 气体灭火模式**：
  - **自动模式时序**：
    1. 模拟火警触发后，控制器响起**声警报**（页面亮起声波脉冲跳动动画，如静音按钮关掉则发出物理合成电子蜂鸣报警声）。
    2. 系统由于高安全性要求，停止在“等待确认”阶段。页面弹出引人注目的“人工确认火警真实性”橙色呼吸发光按钮。
    3. 点击确认后，正式进入 **25 秒倒计时** 撤离期。
    4. 倒计时一开始，**气体分配阀启动开开启**，执行器指示盘在 3 秒内逐渐从 0 度偏转至 90 度，中心管道点亮青色流通发光粒子，呈现出真实的开启动画效果；同时，防爆**声光报警器**的灯罩亮起，警用闪光光束开始旋转扫射。
    5. 倒计时至 0s 时，电磁释放钢瓶阀开启，顶部喷出白色膨胀气体粒子群，精密仪表盘的指针从 **2.0 MPa (满瓶)** 逐渐偏转滑动至 **0.0 MPa (空瓶)**。
    6. 倒计时期间，允许随时点击“紧急终止药剂释放”终止流程，控制闭锁。
  - **手动模式时序**：发生火警后，点击“手动紧急控制释放”，直接瞬间偏转分配阀 100%、拉响警报并释放钢瓶介质。
- **高压细水雾灭火模式**：
  - 支持 5 秒倒计时避险、依次驱动分配阀、联锁启动高压泵组（高压泵风扇开始旋转，卡片微幅物理抖动）、水雾喷头爆碎感温玻泡释放水雾粒子流。
- **组态旁路灰化**：在气体灭火下，细水雾专属的水泵与喷头被置于暗色的旁路状态；在细水雾灭火下，气体释放钢瓶被置于旁路状态，极具专业度。

### 4. 状态机深度优化与记录面板微调（第二轮迭代）
- **状态机重构**：消除了 `soundAlarmActive`（声报警激活）与 `sirenActive`（声光报警激活）两个多余的 React `useState` 状态，改为根据联动控制器核心状态 `status` 进行直接派生计算，防止多头状态控制导致的警报开关不同步或重置残留 bug。
- **计时器稳定性优化**：优化了 `counting` 状态下的倒计时 `useEffect`，使 `setInterval` 在进入倒计时状态时仅创建一次，不再受 `countdown` 数值变化的频繁销毁重创，大幅提高了倒计时计数与分配阀开度动画的同步稳定性。
- **告警记录数微调**：移除了告警控制台右上角多余的 `{sortedAlarms.length} 记录` 标签，精简标题区域，使其布局更加大方简洁，避免与新增的分类统计卡片数字产生视觉重复。

---

## 验证与测试结果

### 静态编译检查
- 运行 `npm run build` 命令，静态 TypeScript 与 CSS 均打包通过，零警告，零错误。
- 经过检查，React Hook 逻辑正常，状态机变换流程流畅无重绘冲突。

### 流程测试截图与操作指南

> [!TIP]
> **演示路径推荐**：
> 1. 打开控制台，导航进入 **告警中心**，测试分类卡片的筛选和火警行漂浮的火星。
> 2. 导航切换到 **灭火联动**，首先选择介质为 `1301气体灭火`，联动触发模式为 `自动模式`。
> 3. 点击 `模拟防护区火警触发`，点击右侧扬声器开启蜂鸣音效。
> 4. 观察左侧弹出 `人工确认火警真实性` 按钮，右侧声报警波纹激活。
> 5. 点击 `确认火警`，验证 25 秒倒计时、分配阀偏转动画、警灯旋转扫射动画。
> 6. 再次重试，测试倒计时 10 秒时点击 `紧急终止` 中止喷射。
> 7. 复位后，切换到 `手动模式` 直接触发手动释放，查看钢瓶瞬间放空归零（2.0MPa $\rightarrow$ 0.0MPa）的动效。
