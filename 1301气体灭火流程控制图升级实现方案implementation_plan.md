# 1301气体灭火流程控制图升级实现方案

本方案旨在根据用户提供的草图与控制逻辑，重构 `src/components/FireLinkage.tsx` 中的 1301 气体灭火工艺流程控制图，实现一个高交互性、具备逼真动态效果的工艺流程仿真图。

## 升级目标

1. **还原工艺拓扑结构**：
   - 包含三个防护区（1301保护区 A、B、C）及其对应的分配阀（分配阀 A、B、C）。
   - 包含三个灭火剂钢瓶站（灭火站 A、B、C），钢瓶通过软管接入主集流排。
   - 包含两个总管隔离阀（总管隔离阀 1、总管隔离阀 2）。
2. **实现联锁联动逻辑仿真**：
   - **保护区 A 火警**：默认开启分配阀 A 及灭火站 A 瓶头阀。若灭火站 A 耗尽，自动开启总管隔离阀 1 并启动灭火站 B 瓶头阀；若 B 也耗尽，开启总管隔离阀 1 与 2 并启动灭火站 C 瓶头阀。
   - **保护区 B 火警**：默认开启分配阀 B 及灭火站 B 瓶头阀。若灭火站 B 耗尽，若 A 有药剂则开启总管隔离阀 1 与灭火站 A 瓶头阀支援，若 C 有药剂则开启总管隔离阀 2 与灭火站 C 瓶头阀支援。
   - **保护区 C 火警**：默认开启分配阀 C 及灭火站 C 瓶头阀。若灭火站 C 耗尽，自动开启总管隔离阀 2 并启动灭火站 B 瓶头阀；若 B 也耗尽，开启总管隔离阀 2 与 1 并启动灭火站 A 瓶头阀。
3. **高颜值的动态 SVG 工艺图**：
   - 在 `spraying`（喷洒）等阶段，根据当前的火警防护区和气源供应站，动态高亮管网流经路径，并使用虚线流动动效展示气流方向。
   - 动态旋转被启用的分配阀手轮与总管隔离阀。
   - 电磁瓶头阀在激活时呈现发光闪烁，钢瓶显示压力表衰减动画。

---

## 拟引入的新 React 状态

为支持多保护区和药剂状态切换，将在 `FireLinkage.tsx` 中引入以下状态：

```typescript
// 当前火警触发的目标保护区
const [targetZone, setTargetZone] = useState<"A" | "B" | "C">("A");

// 各灭火站的药剂瓶组储压状态，用于模拟耗尽工况
// 灭火站 A (1301): pressuresA
// 灭火站 B (1301, 在气体灭火模式下我们将B也作为1301): pressuresB
// 灭火站 C (1301): pressuresC
// 我们将允许用户在界面上“手动设置”哪些站药剂已耗尽 (Depleted)，或者直接通过重置/释放进行气压衰减
```

---

## 提议修改的文件

### [MODIFY] [FireLinkage.tsx](file:///m:/TOPFIRE/TopFireAI/src/components/FireLinkage.tsx)

1. **状态声明与联动逻辑**：
   - 在 1301 模式下，将灭火站 B 从细水雾状态切换为 1301 气体状态（当 `method === '1301'` 时）。
   - 根据当前的 `targetZone` 和各灭火站（pressuresA/B/C）的储压情况，自动计算：
     - 当前正在释放的源灭火站（Source Station）。
     - 总管隔离阀 1 和 2 的状态（`valve1Open`, `valve2Open`）。
     - 分配阀 A/B/C 的开启状态。
     - 气流在 SVG 管网中的流动路径（Path）。
2. **SVG 工艺流程图绘制**：
   - 使用 Y = 90 作为主管线，绘制一条贯穿左右的集流管网。
   - 绘制三个钢瓶，分别位于 X = 470 (A), X = 330 (B), X = 130 (C)，用弯曲的软管连接到主管。
   - 绘制两个隔离阀，分别在 X = 400 (隔离阀 1), X = 200 (隔离阀 2)。
   - 绘制三个分配阀及防护区喷头：
     - Zone A：X = 540 (向下分支，连至分配阀 A -> 保护区 A)
     - Zone B：X = 270 (向上分支，连至分配阀 B -> 保护区 B)
     - Zone C：X = 70 (向下分支，连至分配阀 C -> 保护区 C)
   - 气流管路高亮逻辑：在 `spraying` 状态下，沿着气源站到目标防护区的集流排路径绘制发光气流。
3. **控制面板增强**：
   - 在 SVG 图表下方或上方添加一个“联动逻辑测试控制台”：
     - 按钮选择火警触发区域（A、B、C 保护区）。
     - 按钮/开关手动模拟灭火站 A、B、C 的药剂充盈/空置状态，以便操作员直观测试跨区支援联动效果。
     - 原有的倒计时、手动/自动紧急释放、中止、复位按钮与这套多保护区控制逻辑完全打通。

---

## 验证方案

### 手动验证
- 打开前端页面，切换到“灭火联动”页面，选择 “1301 气体灭火” 介质。
- 在“联动逻辑测试控制台”中：
  - 测试 **正常工况（Zone A 火警，A站有药）**：点击模拟火警，确认只有分配阀 A 和钢瓶 A 开启，隔离阀 1 和 2 处于常闭锁定。
  - 测试 **二级支援（Zone A 火警，A站无药，B站有药）**：设置灭火站 A 药剂已耗尽，触发火警。确认系统自动开启了分配阀 A、总管隔离阀 1 以及灭火站 B 钢瓶，气流从 B 流向 A。
  - 测试 **三级支援（Zone A 火警，A站与B站均无药，C站有药）**：设置 A、B 站均无药，触发火警。确认系统自动开启了分配阀 A、总管隔离阀 1 和 2 以及灭火站 C 钢瓶，气流从 C 流经隔离阀 2 和 1 流向 A。
  - 测试 Zone B 与 Zone C 的火警及其跨区支援流向。
