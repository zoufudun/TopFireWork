# 回路拓扑与网络拓扑图实现说明文档

我们已成功开发了“回路拓扑”与“火灾报警显示控制器网络图”功能，并依据最新需求，对回路接线图的**节点造型（3D立体化）**与**实际安装布线分支拓扑（树形混合敷设）**进行了高标准升级。整个项目已顺利通过编译校验与打包构建。

---

## 最新变更与功能架构

### 1. 形象、立体的 3D 炫丽设备图标
为了让现场点位在数字孪生画面中更加美观与易读，我们为各类回路设备定制了**精美立体的 SVG 3D 图标**：
- **烟温探测器（圆型）**：采用球状 3D 渐变冷色 Dome 外壳，配合对称的风眼通风口及背部阴影。中心配有圆点状态指示灯，在告警时会触发红光呼吸闪烁。
- **手动报警按钮（箱型）**：采用大红色的立体金属火警拉闸箱外形。正中包含白底防误触玻璃窗口以及内部红色的火焰告警标识，底部配有状态指示灯。
- **控制模块（CT）与监视模块（CR）（正方形）**：采用双层斜角金属卡槽机箱外形，顶部配有金黄色的接线铜柱端子。CT 模块以橙色金属框包边，CR 模块以蓝色金属框包边。
- **状态 LED 指示灯闪烁动画**：故障或火警触发时，3D 图标中的核心 LED 指示灯将触发高效的闪烁动画，营造真实且充满科技感的视觉体验。

### 2. 真实物理混合安装布线链 (Mixed Cabling Line)
- 回路设备的地址（1-125 为探测器，126-250 为模块）仅作为逻辑通信地址，不代表物理安装的敷设顺序。
- 物理布线接线图采用真实的混连方式展示，按实际布线路由串联：`探测器 -> 探测器 -> 手动报警按钮 -> 探测器 -> CT模块 -> CR模块 -> 探测器`，更加贴近楼宇现场管线施工的实际拓扑。

### 3. 主干线父节点与树形分支走线
- **控制器出线源（Parent Node）**：在第一分支（Column 1）的顶部，放置了“**控制器回路出线端**”大卡片作为整条回路的物理源头（父节点），显示当前出线电压等运行状态。
- **出线并联分支**：第三分支和第四分支在顶部（Row 0）直接从该父节点并联分出，配有向右延伸的水平金属汇流线。
- **节点下级分支**：第二分支从第一分支的 **第 3 个物理节点（即 Row 3 级的手动报警按钮）** 处分出。接线图在第 3 行绘制了从第一列向第二列引出的虚线分支线与分线箭头，并带有水平流向动画，随后沿着第二分支继续向下敷设各级设备。

---

## 变更文件摘要

- **[components/LoopTopology.tsx](file:///m:/TOPFIRE/TopFireAI/src/components/LoopTopology.tsx)**：
  - 新增 `renderThreeDIcon` 函数，用以绘制精美的 SVG 3D 球体、火警盒与铜端子接线盒。
  - 重构接线表格，第一行（Row 0）渲染“控制器回路出线端”父节点，并加入向分支三、分支四延伸的并联水平线。
  - 在第 3 行加入从分支一指向分支二的水平分支引导虚线及流光动效。
  - 重组设备卡片样式，配合 3D 图标进行左右弹性布局，展示地址、状态、传感器实时遥测值等。
- **[styles.css](file:///m:/TOPFIRE/TopFireAI/src/styles.css)**：
  - 增加 3D 节点外壳 `.three-d-node`、阴影、金黄接线端子与 3D 高光渐变色配置。
  - 增加 LED 状态灯闪烁动画 `.led-flash-alarm`。
  - 增加水平分支线 `.horizontal-branch-line`、水平流动光点 `.horizontal-pulse-dot`、分支指向箭头 `.split-arrow` 等核心管道布线样式。

---

## 验证结果

1. **类型检查验证**：
   ```bash
   npm run typecheck
   ```
   **结果**：100% 成功通过。

2. **生产构建验证**：
   ```bash
   npm run build
   ```
   **结果**：客户端环境打包无误，成功输出压缩优化后的 `dist/` 资源包。
