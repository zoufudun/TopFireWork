# 消防安全控制系统：模拟训练与真实只读模式升级方案

此方案旨在将当前的“灭火联动”升级并移入“模拟训练中心”，增加“模拟训练”与“真实现实”的系统登录模式，并收紧真实环境下的操作边界（即：浏览器、业务服务器与消息平台只能接收消防状态，不得向消防控制器下发复位、消音、启动/停止联动等控制指令）。

---

## 1. 核心需求分析

1. **模拟训练中心与延时灭火联动**：
   - 将“灭火联动”页面（`FireLinkage.tsx`）重命名或重塑为“模拟训练中心”，用作联动灭火过程的模拟与演练。
   - 增加延时联动灭火的配置与模拟（支持对 1301 气体延时和高压细水雾延时进行动态调节）。
2. **只读安全边界与控制闭锁**：
   - 回路拓扑（`LoopTopology.tsx`）在**真实现实**模式下只用于显示实际系统拓扑和读取设备参数，完全禁止任何火警触发、消音和复位控制操作。
   - 浏览器、业务服务器和消息平台在真实状态下不得向控制器下发指令。
3. **用户登录界面（模拟训练 vs 真实现实）**：
   - 增加一个具有现代工控科技感、视觉效果 premium 的登录界面，提供“模拟训练（SIMULATION TRAINING）”与“真实现实（REAL WORKSPACE）”两种权限模式的选择，并记录操作员身份。

---

## 2. 方案设计与变更点

### 2.1 状态管理扩展 ([useFireStore.ts](file:///m:/TOPFIRE/TopFireAI/src/store/useFireStore.ts))

- 增加 `loginMode: "training" | "real" | null` 全局状态。
- 增加 `operatorName: string` 状态。
- 增加 `login: (mode: "training" | "real", operatorName: string) => void` 动作。
- 增加 `logout: () => void` 动作。
- 扩展 `useFireStore` 状态，对 `reset` 等底层触发逻辑加锁或阻断，在 `real` 模式下拦截修改物理控制器状态的事件。

### 2.2 用户登录界面 ([Login.tsx](file:///m:/TOPFIRE/TopFireAI/src/components/Login.tsx) - [NEW])

- 创建一个全屏、高对比度、工控拟态的登录界面：
  - 渐变暗黑科技背景，带微光动画和锁具图标。
  - 操作员签名/姓名输入。
  - 两个带有发光边框的精美模式选择卡片：
    1. **模拟训练模式 (SIMULATION)**：开启火警注入、故障模拟、声光警报消音和联动复位演示，供演练使用。
    2. **真实现实模式 (REAL STATUS)**：物理控制总线单向闭锁，系统仅提供只读监控拓扑和设备状态，阻断消音、复位及联动触发指令下发。

### 2.3 模拟训练中心 ([FireLinkage.tsx](file:///m:/TOPFIRE/TopFireAI/src/components/FireLinkage.tsx) 与延时升级)

- 将导航栏原“灭火联动”更名为“模拟训练中心”。
- 在组件顶部的控制配置栏增加“联动延时参数配置”：
  - **1301 气体联动延时**：可通过 Slider 调节（5s - 60s），控制火警确认后的防护区人员撤离倒计时。
  - **高压细水雾联动延时**：可通过 Slider 调节（0s - 30s），控制确认后的倒计时时序。
- **只读闭锁提示**：如果系统当前处于“真实现实”模式，则在模拟训练中心界面放置一块半透明的高级物理锁闭防尘玻璃罩（Overlay），提示用户“当前处于真实现实安全只读模式，已闭锁所有联动指令，请切换到模拟训练模式进行仿真与延时模拟”。

### 2.4 回路拓扑控制闭锁 ([LoopTopology.tsx](file:///m:/TOPFIRE/TopFireAI/src/components/LoopTopology.tsx))

- 在“现场设备模拟控制台”区域，当 `loginMode === "real"` 时：
  - 将模拟按钮（模拟火警触发、模拟通信故障、消音复位信号）全部隐藏或替换为禁用状态。
  - 增加醒目的安全锁定状态条 `🔒 物理控制网络已闭锁 (真实现实只读监控中)`，并加入免责及安全警示说明。

### 2.5 全局 App 整合 ([App.tsx](file:///m:/TOPFIRE/TopFireAI/src/App.tsx))

- 渲染逻辑：如果 `loginMode === null`，渲染 `<Login />`。
- 侧边栏与头部：
  - 侧边栏“灭火联动”更名为“模拟训练中心”。
  - 侧边栏底部的“模拟数据流”卡片：在 `real` 模式下隐藏启动/暂停/重置按钮，改为显示 `🔒 真实数据流同步中`。
  - 侧边栏底部展示当前登录的操作员名及所选权限模式，并提供“退出登录 / 切换模式”的红色操作按钮。
  - 头部“演示模式”通知栏在 `real` 模式下替换为“🔒 安全只读模式已生效：本系统不具备下发指令至消防控制器的物理通路”。
- 全局火警联锁弹窗（Blasting Prompt）：仅在 `training` 模式下弹出；在 `real` 模式下如果触发了火警，只在告警中心提示，不弹出具有“前往控制中心执行”性质的指令触发提示层，或将其改造为纯警告通知。

---

## 3. 验证方案

### 3.1 手动验证
- **登录阶段验证**：
  - 启动后进入登录页，输入“李工”，选择“模拟训练模式”登录。验证侧边栏和主界面处于模拟状态。
  - 点击侧边栏“退出登录”，切换为“真实现实模式”登录，操作员设为“张总监”。
- **真实现实（安全只读）边界验证**：
  - 进入“回路拓扑”页面，选择任意回路设备，验证右下侧的模拟火警、模拟故障、消音复位按钮全部被物理禁用/隐藏，并有闭锁锁头提示。
  - 进入“模拟训练中心”页面，验证出现只读提示，所有手动模拟触发、释放中止、安全复位、重装按钮全被遮罩物理锁闭。
  - 验证侧边栏的“模拟数据流”控制器隐藏，处于真实接收状态。
- **模拟训练中心延时验证**：
  - 在“模拟训练模式”下，进入“模拟训练中心”，在顶部修改延时参数为 12s。
  - 点击“模拟防护区火警触发”，再点击“确认火警”，验证撤离延时倒计时正确从 12s 开始递减，并在归零后顺利点亮雾化泵/电磁释放阀，演示灭火过程。

### 3.2 自动化构建与 Lint 检查
- 运行 `npm run build` 确保 TypeScript 类型与 React 组件编译完全无误。
