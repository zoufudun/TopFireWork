# 灭火联动与工艺流程深度升级实现方案

本方案旨在针对**消防联动系统**进行第二阶段重构与功能扩展。包括：与回路火警事件进行深度联动、提升细水雾高压喷射粒子特效、设计开发灭火站钢瓶监测页面（包含三个站、7个容量不同的钢瓶及逼真释放动画）、开发防护区管网监测页面（包含多个防护区对应的分配阀与压力开关动态状态）。

## 用户审核项

> [!IMPORTANT]
> **新子页面布局结构**
> 1. 我们将在 `FireLinkage` 控制台的顶部引入一个高级子导航栏，包含三个工作选项卡：
>    - **联动控制中心** (Linkage Control) - 当前的系统联动状态机与时序图控制台。
>    - **灭火剂储存站** (Extinguish Stations) - 展示 A、B、C 三个灭火站的 7 瓶组三维仿真（1#为短粗 40L 罐，2-7#为高大 85L 罐）。
>    - **防护区管网监测** (Zone Valve Network) - 监视多个防护区部位阀门及压力开关的开启状态。
> 2. 与回路火警的联动将通过 `eventBus` 的 `"alarm:created"` 消息自动触发。一旦检测到火警事件：
>    - 若发生于 B1 配电间等电气设备区，自动将介质切为 `1301气体`。
>    - 若发生于 F3/F2 等办公会议区，自动将介质切为 `高压细水雾`。
>    - 若为自动联动模式，将自动切换到对应介质并触发联动流转（例如弹出“人工确认”或直接开阀）。

## 待讨论问题

> [!NOTE]
> **关于联动触发的提示**
> 当后台回路或模拟数据流中产生真实火警时，为了确保用户能够感知到发生了自动联动，灭火联动页面会弹出一个炫酷的“火警联锁驱动中”提示层，指引操作员点击确认或者查看联动流程。

---

## 拟作出的修改

### Component: Fire Linkage Redesign (灭火联动升级)

#### [MODIFY] [FireLinkage.tsx](file:///i:/TOPFIRE/TopFireAI/src/components/FireLinkage.tsx)
1. **子页面导航管理**：
   - 增加 `subTab` 状态，控制显示：“联动控制”、“储存站监视”、“防护区阀门组监视”。
2. **细水雾高压喷射效果优化**：
   - 在细水雾喷头 SVG 中，增加高压射流的重叠雾化渐变图层，增加粒子数量至 25 个。
   - 在 CSS 中为 `.mist-particle` 增加不同初速度、更大横向漂移阻尼，利用模糊滤镜 `blur(0.5px)` 模拟极具真实感的高压雾化射流。
3. **回路设备火警自动联动逻辑**：
   - 使用 `useEffect` 注册 `eventBus.on("alarm:created")` 消息。
   - 解析警报 payload，如果是火警且系统处于 `safe` 状态：
     - 根据报警的 `floorId` 自动判断手段：
       - `floorId === "B1"`：切换灭火手段为 `1301气体`
       - `floorId === "F2"` 或 `"F3"`：切换灭火手段为 `高压细水雾`
     - 调用 `triggerFireSim()`，自动拉响声报警，按照设定的模式（自动/手动）自动流转状态机。
4. **灭火剂储存站（A/B/C 站及钢瓶）实现**：
   - 设计“储存站监视”子页面，包含 A、B、C 三个灭火站的卡片切换。
   - 每个站通过 SVG 渲染 7 个直立排列的钢瓶：
     - 1号钢瓶（ZS40 40L）：高 70px，宽 18px，代表短小充装瓶。
     - 2至7号钢瓶（ZS85 85L）：高 92px，宽 22px，代表高大主钢瓶。
     - 包含精美细节：瓶口黄铜释放阀、集流管铜管、气压检测表盘。
     - 动效：当该灭火站对应的介质释放时（例如 A站/B站对应1301气体喷洒），释放时集流管喷出白烟，钢瓶气压表指针偏转，释放后压力为 0。
5. **防护区管网与压力开关监视实现**：
   - 设计“防护区监视”页面，列出 5 个保护区（"3F东侧办公区", "3F西侧办公区", "2F会议大厅", "B1配电间", "B1泵房"）。
   - 每个部位均配有一个专属的“电磁阀阀体组”和“压力开关”SVG。
   - 实时根据联动状态机的驱动显示对应的阀门开启（阀芯旋转为绿色流通）和压力开关动作（常开接点闭合，电路通路点亮红色/黄色发光指示）。

#### [MODIFY] [styles.css](file:///i:/TOPFIRE/TopFireAI/src/styles.css)
- 增加灭火站钢瓶排布、粒子发生器的 CSS 动画类。
- 增加高压喷雾的喷气喷雾雾化动画特效。
- 增加防护区阀门旋转、压力开关开合相关的闪烁发光动画效果。

---

## 验证计划

### 自动测试与静态检查
- 运行 `npm.cmd run build` 验证打包无 TypeScript 错误。

### 手动验证方案
- **细水雾高压特效验证**：进入“联动控制中心”触发细水雾释放，验证喷雾是否浓密、射流效果是否逼真。
- **回路火警自动联动验证**：
  1. 切换到“回路拓扑”页面，手动触发某个设备的火警（或等待模拟器火警生成）。
  2. 切换回“灭火联动”页面，验证系统是否已自动触发声警报，并已自动根据楼层切换为气体（B1）或细水雾（F3）。
- **灭火剂储存站验证**：
  1. 切换到“灭火剂储存站”子页面，切换 A/B/C 站，验证瓶身 40L 与 85L 比例。
  2. 触发灭火释放，验证对应灭火站的钢瓶集流管是否有气体溢出、气压归零的动画。
- **防护区监视验证**：
  1. 切换到“防护区管网监测”页面，验证 5 个保护区的阀门和压力开关状态。
  2. 触发联动时，对应的防护区电磁阀是否有开启动画，管网压力开合指示是否亮起。
