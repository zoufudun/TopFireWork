# 回路拓扑与网络拓扑图实现说明文档

我们已成功开发了“回路拓扑”与“火灾报警显示控制器网络图”功能，并在近期重构中针对您的最新诉求进行了针对性优化：

---

## 最新优化成果

### 1. 彻底解决拓扑图画面闪烁问题（满足要求一）
- **静态数组常量提取**：将 `branchNames`、`slots` 等定义移出了 `LoopTopology` 组件的渲染方法主体，放置在顶层作用域，消除了由于数据更新导致数组重新声明、进而触发的无谓渲染重绘。
- **React DOM 节点稳定 Key 锚定**：
  - 在渲染三维设备卡片时，为其配置了唯一的物理卡片 Key 属性（`key={\`dev-card-\${dev.id}\`}`）。
  - 给各个分支间的直角线（SVG）和垂直连接电缆（styled wrapper）同样附加了稳定的、基于格点的 Key 标记（如 `split-b2-\${rowIdx}`、`vwire-\${rowIdx}-\${colIdx}`）。
  - 即使模拟器在秒级频繁推送指标遥测变化（引起 state 频繁重绘），React 也能在协调阶段完美复用已存在的 DOM 结构和 SVG 容器，**避开了重算高度、重设动画计时等任何导致画面闪烁、动画断点抖动的浏览器行为**。通信脉冲依然行流水般自然滚动。

### 2. 完美的网络连接图 3D 图标化与空间布局优化（满足要求二）
- **防止溢出的宽幅布局设计**：我们将 `renderNetworkSvg` 的 SVG 画布比率从原先的 `600 x 420` 扩展优化为 `800 x 500`。
- **避让排列算法**：重新规划了控制器柜、 LCM 回路通信板以及回路标识节点在空间中的平面位置。对各个节点从左、右、下进行了均衡间距拓展，彻底解决了之前版本中由于模块线过长导致的网络图左右出界、内容显示不全、元素重叠的布局缺陷。
- **形象 3D 物理机柜图标**：网络图中的控制器不再采用扁平的圆圈，而是升级为**高保真 3D 工业机柜 SVG 矢量图形**。通过左侧、右侧、顶部三个轴测切面的渐变阴影对比塑造出机箱立体效果，机箱正面绘制了层级插槽，并在左前方面板嵌入了绿、蓝、红交替闪烁的通信 LED 指示灯，选中时外围更是环绕着一圈立体的网络光晕圈。
- **形象 3DLCM模块图标**：下层连接的 LCM 模块也升级为**3D 通信板卡外壳图标**。带有黄金配色的电缆接线端子排、发光的 LCM 物理文字标识以及贴片运行指示灯，让复杂的火灾网络连接关系一眼望去即可感悟出工业的严谨与精美。

---

## 变更文件摘要

- **[components/LoopTopology.tsx]**：
  - 将 slots、branchNames 以及 maxRows 规范为顶层静态不可变对象。
  - 为所有直角布线组件、垂直电缆容器和设备网格卡片补充稳定的 `key` 属性值，避免 React 在 telemetry 推送时重载节点导致闪烁。
  - 调整 `renderNetworkSvg` 为 800x500 画布，重算控制器柜与 LCM 模块的三维坐标；新增 `renderNetworkControllerIcon` 和 `renderNetworkModuleIcon` 两款精细的 3D 三面轴测矢量渲染流程。

---

## 验证结果

1. **类型检查验证** (`npm run typecheck`)：
   - **结果**：100% 成功通过。

2. **生产构建验证** (`npm run build`)：
   - **结果**：打包完全无误，生产资源包已完美编译。
