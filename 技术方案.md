<think> **Planning Chinese fire safety architecture**

<!-- -->
</think>
有。相比“单页面炫酷仪表盘”，更推荐采用**数字孪生可视化 + 事件驱动架构 + 分级告警工作台**。既保持超科技视觉，又兼顾信息密度、可维护性和真实业务流程。

> 重要边界：网页系统适合作为监控、辅助研判和管理平台，不能替代经认证的火灾报警控制器及消防联动控制设备。AI 不应未经人工确认直接执行消防联动。

---

## 一、推荐技术方案

### 前端技术栈

```txt
React 18/19 + Vite
├─ JSX
├─ React Router            页面与权限路由
├─ Zustand                 实时告警、设备与界面状态
├─ TanStack Query          API 请求、缓存与重试
├─ ECharts                 趋势图、设备统计、风险分析
├─ SVG / Konva.js          楼层平面图和设备点位
├─ Framer Motion           状态切换动画
├─ React Hook Form + Zod   表单和数据校验
├─ Axios                   REST API
├─ WebSocket / MQTT over WS 实时事件
└─ MSW                     无后端时模拟接口
```

如果是正式长期项目，建议使用 **TypeScript + TSX**。如果必须严格使用 JSX，也可以通过 JSDoc、Zod 和 ESLint 保证数据可靠性。

### 后端与数据层

```txt
浏览器监控端
     │
HTTPS / WebSocket
     │
业务 API 网关
     ├─ 身份认证与权限控制
     ├─ 告警事件服务
     ├─ 设备管理服务
     ├─ 工单与处置服务
     ├─ 审计日志服务
     └─ 消息推送服务
              │
       隔离的数据采集网关
              │
   经认证的消防报警及联动设备
```

前端不要直接连接或控制消防设备。应通过经过安全隔离、身份认证和协议适配的数据网关读取状态。

---

## 二、比当前方案更合理的页面结构

### 1. 态势总览

顶部只保留关键指标：

- 当前火警数量
- 未确认告警数量
- 故障设备数量
- 屏蔽或隔离设备数量
- 设备在线率
- 最近一次数据更新时间
- 主备链路状态

中央显示园区或建筑数字孪生视图，右侧显示按照优先级排序的事件流。

### 2. 告警处置工作台

不要只展示“告警卡片”，应形成完整处置流程：

```txt
事件发生
→ 系统接收
→ 值班员确认
→ 现场复核
→ 启动预案或排除误报
→ 处置完成
→ 生成不可篡改的事件记录
```

每个事件建议包含：

- 事件等级与类型
- 首次发生时间、持续时间
- 建筑、楼层、分区及设备地址
- 关联设备与附近摄像头
- 当前值班员
- 处置计时
- 操作记录
- 现场反馈
- 预案建议
- 数据来源及可信度

### 3. 楼层数字孪生

使用 SVG 或 Konva.js 绘制楼层平面图，设备点位分层显示：

- 烟感、温感、火焰探测器
- 手动报警按钮
- 消火栓按钮
- 防火门
- 防排烟设备
- 消防广播
- 声光报警器
- 喷淋与水流指示器
- 疏散通道和安全出口

建议支持：

- 缩放与拖动
- 按设备类型筛选
- 故障、火警、离线状态闪烁
- 点击点位打开设备详情
- 告警传播范围高亮
- 疏散路线展示
- 历史事件回放

### 4. 设备运维中心

设备状态不要只展示在线率，还应包括：

- 在线、离线、故障、屏蔽、维护
- 最后心跳时间
- 通信质量
- 电池或电源状态
- 最近检测时间
- 维护周期
- 故障趋势
- 工单状态
- 历史报警次数

### 5. 历史分析

推荐增加：

- 按时间、建筑、楼层、设备查询
- 告警数量趋势
- 平均确认时间
- 平均处置时间
- 误报率
- 高频故障设备
- 告警热力图
- 报表导出

---

## 三、推荐的超科技视觉方向

不建议整个界面都使用高亮霓虹，否则长时间值守容易疲劳。更好的方式是：

### 色彩语义

```css
--bg-primary: #050912;
--bg-panel: #0a1220;
--border: rgba(93, 225, 255, 0.16);

--normal: #32e6b0;
--info: #39c6ff;
--warning: #ffb547;
--alarm: #ff4d4f;
--fault: #b388ff;
--offline: #75839a;
```

颜色必须表达稳定含义：

- 红色：真实火警或紧急事件
- 橙色：预警
- 紫色：设备故障
- 灰色：离线或未知
- 青绿色：正常

避免把大量普通按钮也做成红色，否则会削弱真实火警的视觉优先级。

### 动效原则

建议使用：

- 新告警边框脉冲
- 数据更新轻微数字滚动
- 建筑楼层风险渐变
- 告警定位扫描线
- 面板进入的低幅度过渡

不建议使用：

- 全屏持续闪烁
- 大量背景粒子
- 频繁旋转动画
- 无业务意义的雷达扫描

同时提供“减少动态效果”选项，兼顾值守人员和无障碍需求。

---

## 四、推荐的数据模型

统一告警数据模型非常重要：

```jsx
const alarm = {
  id: "ALM-20250308-0001",
  sourceId: "DET-A-03-021",
  category: "FIRE",
  severity: "CRITICAL",

  location: {
    campus: "总部园区",
    building: "A栋",
    floor: "3F",
    zone: "东侧走廊",
    coordinates: { x: 428, y: 176 }
  },

  status: "UNACKNOWLEDGED",
  occurredAt: "2025-03-08T10:28:43+08:00",
  receivedAt: "2025-03-08T10:28:44+08:00",
  acknowledgedAt: null,
  resolvedAt: null,

  measurements: {
    smoke: 72,
    temperature: 45
  },

  operator: null,
  confidence: 0.94,
  source: "CERTIFIED_GATEWAY",
  correlationId: "EVT-88901"
};
```

状态建议明确设计成状态机：

```txt
UNACKNOWLEDGED
→ ACKNOWLEDGED
→ VERIFYING
→ PROCESSING
→ RESOLVED
→ CLOSED
```

不要允许任意跳转。例如未确认事件不能直接关闭。

---

## 五、推荐项目目录

```txt
src/
├─ app/
│  ├─ App.jsx
│  ├─ router.jsx
│  └─ providers.jsx
├─ assets/
├─ components/
│  ├─ ui/
│  ├─ charts/
│  ├─ alarms/
│  ├─ floor-map/
│  └─ devices/
├─ features/
│  ├─ dashboard/
│  ├─ alarm-center/
│  ├─ digital-twin/
│  ├─ device-center/
│  ├─ maintenance/
│  └─ reports/
├─ layouts/
│  ├─ ConsoleLayout.jsx
│  └─ FullscreenLayout.jsx
├─ services/
│  ├─ api.js
│  ├─ alarms.js
│  ├─ devices.js
│  └─ realtime.js
├─ stores/
│  ├─ alarmStore.js
│  ├─ deviceStore.js
│  └─ sessionStore.js
├─ mocks/
│  ├─ handlers.js
│  └─ realtimeSimulator.js
├─ hooks/
├─ schemas/
├─ utils/
└─ styles/
   ├─ tokens.css
   ├─ global.css
   └─ animations.css
```

这种结构比把所有内容放在一个 `App.jsx` 中更利于后续扩展。

---

## 六、实时数据实现建议

### WebSocket 消息格式

```json
{
  "type": "alarm.created",
  "eventId": "EVT-88901",
  "timestamp": "2025-03-08T10:28:44+08:00",
  "payload": {
    "alarmId": "ALM-20250308-0001",
    "severity": "CRITICAL",
    "deviceId": "DET-A-03-021",
    "building": "A栋",
    "floor": "3F"
  }
}
```

前端需要处理：

- 心跳检测
- 自动重连与指数退避
- 消息去重
- 消息顺序校验
- 断线提示
- 断线后的增量补偿
- 浏览器时间与服务器时间偏差
- 同一事件多设备关联

不建议使用纯随机定时器模拟正式业务。演示阶段可以使用 MSW 和模拟 WebSocket，但页面必须醒目标注“模拟数据”。

---

## 七、AI 功能的合理定位

AI 可以提供：

- 多传感器事件关联
- 告警摘要
- 设备故障趋势预测
- 高频误报点识别
- 相似历史事件检索
- 预案和处置步骤提示

AI 不应：

- 自动判定真实火灾已解除
- 自动关闭告警
- 绕过值班员确认
- 未经授权直接控制消防联动设备
- 隐藏或降低原始设备告警等级

AI 输出应始终显示：

```txt
辅助研判结果，不替代现场确认和法定消防处置流程
```

---

## 八、权限与安全设计

至少应设置：

| 角色       | 权限                   |
| ---------- | ---------------------- |
| 观察员     | 查看实时状态           |
| 值班员     | 确认告警、填写处置记录 |
| 主管       | 审核事件、查看报表     |
| 维护人员   | 管理设备与工单         |
| 系统管理员 | 用户、角色和系统配置   |
| 审计员     | 只读查看操作日志       |

关键操作建议采用：

- 二次确认
- 原因必填
- 操作者身份记录
- 时间戳记录
- 不可直接删除审计日志
- 会话超时与重新认证
- 最小权限原则

正式项目还应结合适用的中国法律法规、网络安全和数据安全要求，并由消防专业机构依据现行标准完成设计审查、设备选型、集成测试和验收。可重点核验现行版本的火灾自动报警系统设计、控制器及消防联动控制相关国家标准。

---

## 九、最佳实施路径

推荐按四个阶段开发：

1. **原型阶段**：总览、告警中心、SVG 楼层图、设备状态、模拟数据。
2. **业务阶段**：登录权限、告警状态机、工单、历史查询、审计日志。
3. **集成阶段**：接入隔离网关、WebSocket 实时数据、断线补偿、数据校验。
4. **验证阶段**：安全测试、压力测试、故障演练、专业消防集成测试与验收。

综合来看，最值得优先升级的是：**组件化工程结构、SVG 楼层数字孪生、真实告警状态机、WebSocket 事件层、RBAC 权限和完整审计日志**。这会比单纯继续增加视觉特效更有价值。