# 回路拓扑与网络拓扑图实现说明文档

我们已成功开发了“回路拓扑”与“火灾报警显示控制器网络图”功能，并且根据最新要求，将回路物理拓扑重构为**加大显示的垂直分支表格拓扑结构**。

---

## 最新变更与功能架构

### 1. 垂直分支布线表格拓扑
为了使回路接线更直观、贴近实际工程结构，我们将原有的横向单链流图重构为了**交互式垂直表格分支拓扑**：
- **横轴方向（表格列）**：代表不同的通信及供电分支，分为 4 条分支：
  - **分支 1**：低区烟温感探头链 (地址 1 - 60)
  - **分支 2**：高区烟温感探头链 (地址 61 - 125)
  - **分支 3**：手动报警按钮链 (地址 126 - 190)
  - **分支 4**：输入输出控制模块链 (地址 191 - 250)
- **纵轴方向（表格左侧）**：代表回路设备在分支内的**物理安装深度 / 顺序**，划分为第 1 到 8 个物理安装位置层级。
- **设备连接线与动态信号流向**：
  - 每一列（分支）背景中均有纵向敷设的通讯总线电缆。
  - 每个分支内的设备卡片之间，有自上而下的**发光信号脉冲波**沿总线向下移动，代表控制器循环巡检的物理信号。
  - 脉冲波根据列中设备的当前健康状态而呈现不同的颜色（绿色正常、红色火警、黄色故障、蓝色离线），且设置了基于行序号的级联延迟（Cascading Delay），呈现完美的向下倾泻效果。
  - 顶部设计了“回路出线端总线”及引出的各分支汇流接线点，整体展示出优美的工业科技质感。

### 2. 统计卡片与回路独立指标计数
- 顶部大卡片继续展示全局 5 项统计：**总设备数、在线数、报警数、故障数（Warning 态）、离线数**。
- 在“回路物理地址拓扑”的工具栏中，新增了**回路数据汇总条**。每当切换不同的控制器与回路通道时，该汇聚条会实时统计并更新当前选中回路的这 5 项指标（总数、在线数、报警数、故障数、离线数），完全满足了多级精确监控的需求。

---

## 变更文件摘要

- **[types.ts](file:///m:/TOPFIRE/TopFireAI/src/types.ts)**：增加了 `"topology"` 路由页，以及 `TopologyDevice`、`FireLoop`、`LoopModule`、`ControllerNode` 等拓扑相关的核心契约类型。
- **[data/mock.ts](file:///m:/TOPFIRE/TopFireAI/src/data/mock.ts)**：生成控制器与各分支高低区地址回路设备的初始模拟数据集。
- **[store/useFireStore.ts](file:///m:/TOPFIRE/TopFireAI/src/store/useFireStore.ts)**：扩展了 Zustand 状态管理，提供控制器、模块与回路的选择器，以及三级手动注入测试（触发火警、触发断线、消音复位）。已通过 explicit-casting 解决了隐式 `string` 拓宽导致的 TypeScript 编译错误。
- **[simulator/simulator.ts](file:///m:/TOPFIRE/TopFireAI/src/simulator/simulator.ts)**：实时 Tick 仿真计算。在此处加入了对回路中各物理设备微小遥测抖动及随机触发故障的动态模拟。
- **[components/LoopTopology.tsx](file:///m:/TOPFIRE/TopFireAI/src/components/LoopTopology.tsx)**：重构了回路拓扑页面，主要结构包含 SVG 环网网络图、顶部 5 大指标统计、回路统计条、分支接线表格、地址状态矩阵以及右侧详细调试区。
- **[App.tsx](file:///m:/TOPFIRE/TopFireAI/src/App.tsx)**：注册路由菜单与 Lucide 导航图标。
- **[styles.css](file:///m:/TOPFIRE/TopFireAI/src/styles.css)**：增加了用于垂直表格分支网络、电缆背景敷设、垂直流动动画、玻璃拟态卡片的科技风格 CSS。

---

## 验证结果

1. **类型检查验证**：
   ```bash
   npm run typecheck
   ```
   **结果**：100% 成功通过。

2. **生产构建验证**：
   ```bash
   npm run build
   ```
   **结果**：客户端环境打包无误，成功输出压缩优化后的 `dist/` 资源包。
