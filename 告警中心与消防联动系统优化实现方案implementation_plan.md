# 告警中心与消防联动系统优化实现方案

本方案旨在针对**告警中心**的事件分类统计与火警显示效果进行视觉及动态化提升，并对**灭火联动控制中心**进行全面重构，以满足火灾联动控制工艺流程及动画展现需求（包括 1301 气体灭火和高压细水雾灭火的自动/手动控制逻辑、25秒倒计时、声光报警动画、分配阀开启动画、钢瓶气压释放及喷洒动画等）。

## 用户审核项

> [!IMPORTANT]
> **联动逻辑细节与音视频资源**
> 1. 本次开发将全面重新设计 `FireLinkage` 面板以符合国家消防规范中的气体灭火联动时序（报警 -> 确认火警 -> 分配阀开启与声光警报 -> 25秒延时 -> 钢瓶释放）。
> 2. 声报警默认在界面中使用视觉动画（声波扩散、警报灯闪烁）来指示，无需外接音频文件，也可加入浏览器 Web Audio API 合成的电子警报声以提高逼真度。

## 待讨论问题

> [!NOTE]
> **关于细水雾灭火模式的倒计时**
> 规范中细水雾灭火系统一般采用湿式、干式或预作用系统。根据用户描述，25秒倒计时和分配阀/钢瓶逻辑为 **1301 气体灭火系统** 独有。为了用户体验的完整性，高压细水雾灭火模式将保留“阀组动作 -> 水泵启动 -> 喷头发射细水雾”的流动动画。

---

## 拟作出的修改

### Component: Alarm Center (告警中心)

#### [MODIFY] [AlarmCenter.tsx](file:///i:/TOPFIRE/TopFireAI/src/components/AlarmCenter.tsx)
- 在告警控制台顶部引入一个高视觉品质的分类计数统计面板（统计项：总数、火警数量、故障数量、状态数量）。
- 统计卡片设计成发光玻璃态（Glassmorphic）卡片，带有悬停微动效、状态呼吸灯，并与 `filterType` 筛选条件进行双向联动：点击相应的统计卡片即可直接切换下方的告警列表过滤。
- 为火警类型的告警行（`alarm-row`）添加 `<div className="spark-container">`，内含 6 个动态漂浮上升并渐隐的火星粒子效果（Spark Particles），强化火灾警报的紧急感。

#### [MODIFY] [styles.css](file:///i:/TOPFIRE/TopFireAI/src/styles.css)
- 增加告警中心统计卡片的 CSS 样式（`.alarm-stats-grid`, `.alarm-stat-card`, 配合不同状态的渐变边框和发光效果）。
- 增加火星粒子漂浮动画 `@keyframes sparkRise` 及对应的粒子定位样式。
- 增加火警行左侧红色闪烁及微幅振动（震颤效果），突出极度紧急的状态。

---

### Component: Fire-fighting Linkage (灭火联动)

#### [MODIFY] [FireLinkage.tsx](file:///i:/TOPFIRE/TopFireAI/src/components/FireLinkage.tsx)
- **参数控制面板**：
  - 新增“灭火手段选择”：**1301 气体灭火** vs **高压细水雾灭火**。
  - 新增“联动灭火模式”：**自动模式** vs **手动模式**。
- **状态机控制逻辑**：
  - `status` 状态扩展为：`"safe" | "alarmed" | "confirming" | "counting" | "spraying" | "aborted" | "resetting"`
  - **触发火警**：开启声警报（声波图标扩散动画，声光闪烁）。
  - **如果是1301气体灭火**：
    - **自动模式**：系统停在确认阶段，要求操作员手动点击“确认火警”。确认后，开启 25 秒倒计时。
    - **倒计时启动时**：同时执行两个动作：
      1. 分配阀开启（状态设为开启中，运行分配阀旋转及进度动画，最终变为常开）。
      2. 开启声光报警器动画（闪烁的红黄防爆警铃灯、扩散光斑）。
    - **倒计时过程中**：允许点击“紧急停止”。点击后，倒计时终止，分配阀锁定关闭，声光报警熄灭，系统进入“紧急中止”状态。
    - **25秒倒计时到 0**：开启钢瓶电磁阀，释放药剂。钢瓶压力瞬间/快速从 2.0 MPa 降到 0.0 MPa，状态从“满瓶”变更为“空瓶”，显示高压气体喷洒出的粒子烟雾效果。
    - **手动模式**：发生火警后，无需确认和 25 秒倒计时，点击“手动启动”直接开启分配阀并喷洒药剂。
  - **如果是高压细水雾灭火**：
    - 可配置手动/自动控制水泵和区域阀。自动模式触发后延迟 5 秒依次开启区域阀、水泵并喷水，手动模式则可直接逐个点控。
- **动画与视觉组件重新设计**：
  - **声光报警器 (Siren/Alarm)**：新增一个 SVG 声光报警器组件，具有警徽形状、警灯旋转扫射和声波向外扩散的动态 CSS 动画。
  - **分配阀组 (Selector Valve)**：重新设计分配阀的 SVG，将阀体与电磁执行机构单独控制。开启时，执行器指示盘旋转 90 度，中心阀道亮起，并有绿色流动光效。
  - **1301钢瓶组 (Cylinder Tank)**：将默认压力调为释放前 2.0 MPa，释放后 0.0 MPa。释放时，钢瓶顶部阀门喷出半透明气体云雾，仪表盘指针由 2.0 滑动归零，钢瓶状态显示“已放空”。
  - **声警报指示**：增加声报警波纹图，直观展现音响状态。

---

## 验证计划

### 自动测试与静态检查
- 在终端运行 `npm run build` 确保无 TypeScript 或 Vite 打包编译错误。
- 确保 React hook 的依赖项完整，无无限重绘问题。

### 手动验证方案
- **告警中心测试**：
  1. 进入“告警中心”页面，验证顶部四个统计卡片的数据正确性。
  2. 点击各个卡片，列表能够过滤出对应的告警数据。
  3. 观察火警行，确认漂浮的红黄色动态火星效果流畅、位置错落。
- **灭火联动测试**：
  1. 切换灭火手段为 `1301气体灭火`，模式为 `自动`，点击 `模拟火警触发`。
  2. 验证声警报是否亮起（有波纹动画）。确认按钮是否在闪烁等待点击。
  3. 点击 `确认火警`，验证 25 秒倒计时是否启动，同时分配阀开启（旋转动画）、声光报警器开始红蓝旋转闪烁。
  4. 在 15 秒时点击 `紧急停止`，倒计时停止，警报中止，系统状态变为紧急中止。点击 `联动安全复位` 回到初始态。
  5. 再次触发并让 25 秒跑完，观察 0 秒时钢瓶压力指针滑向 0Mpa，钢瓶顶部云雾喷射效果是否展现。
  6. 验证 `手动模式` 下，直接点击 `手动紧急启动` 即可忽略确认和倒计时瞬间触发释放。
  7. 切换为 `高压细水雾灭火`，验证原有的高压泵和细水雾喷洒流转逻辑。
